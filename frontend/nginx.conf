server {
  listen 80;

  # Sirve la SPA: cualquier ruta desconocida vuelve a index.html
  # para que el enrutamiento de React funcione.
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}
