apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.pagos.name }}
  labels:
    app: {{ .Values.services.pagos.name }}
spec:
  replicas: {{ .Values.services.pagos.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.services.pagos.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.pagos.name }}
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: {{ .Values.services.pagos.name }}
          image: "{{ .Values.global.image.registry }}/{{ .Values.global.image.owner }}/{{ .Values.services.pagos.image }}:{{ .Values.global.image.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.services.pagos.port }}
          envFrom:
            - configMapRef:
                name: campusconnect-common-config
            - secretRef:
                name: campusconnect-secrets
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
