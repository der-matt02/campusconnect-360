apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.notificaciones.name }}
  labels:
    app: {{ .Values.services.notificaciones.name }}
spec:
  ports:
    - port: {{ .Values.services.notificaciones.port }}
      targetPort: {{ .Values.services.notificaciones.port }}
      protocol: TCP
  selector:
    app: {{ .Values.services.notificaciones.name }}
