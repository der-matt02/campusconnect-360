apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.asistencia.name }}
  labels:
    app: {{ .Values.services.asistencia.name }}
spec:
  ports:
    - port: {{ .Values.services.asistencia.port }}
      targetPort: {{ .Values.services.asistencia.port }}
      protocol: TCP
  selector:
    app: {{ .Values.services.asistencia.name }}
